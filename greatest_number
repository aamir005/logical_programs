<?php
function find_greatest_number($arr){
$greatest = $arr[0];
 for($i=1;$i<count($arr);$i++){
    if($arr[$i] > $greatest){
	  $greatest = $arr[$i];
	}
  }
  
  return $greatest;
}

$m=[4,5,7,3,1,2];
echo "Greatest Number is ".find_greatest_number($m);
?>
